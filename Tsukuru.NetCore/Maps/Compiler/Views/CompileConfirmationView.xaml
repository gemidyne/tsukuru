<UserControl x:Class="Tsukuru.Maps.Compiler.Views.CompileConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding CompileConfirmationViewModel, Source={StaticResource ViewModelLocator}}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" 
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Margin="0,10,0,0">
            <Button Content="Launch map" 
                    Command="{Binding LaunchMapCommand}"
                    Width="124"
                    HorizontalAlignment="Right" />

            <Button Content="Compile" 
                    Command="{Binding MapCompileCommand}"
                    IsEnabled="{Binding IsExecuteButtonEnabled}"
                    Margin="5,0,0,0"
                    Width="124"
                    HorizontalAlignment="Right" />
        </StackPanel>
        
    </Grid>
</UserControl>
